<Window x:Class="Warehouse.Views.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Raport Magazynowy" Height="600" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Raport" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <TextBox Grid.Row="1" Text="{Binding ReportContent, Mode=OneWay}" 
                 IsReadOnly="True" 
                 TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto" 
                 FontFamily="Consolas" 
                 Padding="10" 
                 Background="#F9F9F9"/>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Odśwież dane" Command="{Binding GenerateReportCommand}" Padding="10" Margin="0,0,10,0"/>
            <Button Grid.Column="1" Content="Zapisz do TXT" Command="{Binding SaveToFileCommand}" Background="#2196F3" Foreground="White" FontWeight="Bold" Padding="20,10"/>
        </Grid>
    </Grid>
</Window>